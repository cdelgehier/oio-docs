.. title:: Manage Chunks on RAWX, RESTful API

============
RAWX service
============

The RAWX service is a share-nothing service responsible to store the chunks.
The interface uses a subset of the WebDAV commands augmented with custom headers.

.. include:: rawx_api_calls.rst

